# 🌐 Налаштування локального сервера для доступу з інших комп'ютерів

Інструкція для перетворення вашого ПК на сервер, доступний з інших комп'ютерів у мережі або через інтернет.

---

## ⚠️ Важливо: Безпека та обмеження

**Переваги:**
- ✅ Безкоштовно
- ✅ Повний контроль над сервером
- ✅ Швидкий доступ до даних

**Недоліки:**
- ❌ Потрібно постійно тримати ПК увімкненим
- ❌ Потрібен стабільний інтернет
- ❌ Ризики безпеки (потрібен firewall, SSL тощо)
- ❌ Потрібен статичний IP або DDNS
- ❌ Може бути повільно для користувачів

**Рекомендація:** Для production/demo краще використовувати Railway/Vercel (безкоштовно для демо).

---

## 🏠 Варіант 1: Доступ через локальну мережу (LAN)

### Крок 1: Знайдіть IP адресу вашого ПК

```powershell
ipconfig
```

Знайдіть **IPv4 Address** (наприклад: `192.168.1.100`)

### Крок 2: Налаштуйте Backend

**В `backend/.env`:**
```env
HOST=0.0.0.0  # Слухати на всіх інтерфейсах
PORT=5000
SERVER_URL=http://192.168.1.100:5000  # Замініть на ваш IP
CORS_ORIGIN=http://192.168.1.100:3000,http://localhost:3000
```

**В `frontend/.env`:**
```env
VITE_API_URL=http://192.168.1.100:5000/api
VITE_WS_URL=ws://192.168.1.100:5000
```

### Крок 3: Налаштуйте Windows Firewall

```powershell
# Дозволити вхідні з'єднання на порт 5000 (Backend)
New-NetFirewallRule -DisplayName "VITA-Edu Backend" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow

# Дозволити вхідні з'єднання на порт 3000 (Frontend)
New-NetFirewallRule -DisplayName "VITA-Edu Frontend" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow

# Дозволити PostgreSQL (якщо потрібен доступ до БД з інших ПК)
New-NetFirewallRule -DisplayName "PostgreSQL" -Direction Inbound -LocalPort 5432 -Protocol TCP -Action Allow
```

### Крок 4: Доступ з інших комп'ютерів

**З іншого комп'ютера в тій же мережі:**
- Frontend: `http://192.168.1.100:3000`
- Backend API: `http://192.168.1.100:5000/api`
- Swagger: `http://192.168.1.100:5000/api-docs`

---

## 🌍 Варіант 2: Доступ через інтернет (з інших мереж)

### Крок 1: Налаштуйте Port Forwarding на роутері

**У налаштуваннях роутера:**
1. Відкрийте портал адміністратора роутера (зазвичай `192.168.1.1` або `192.168.0.1`)
2. Знайдіть "Port Forwarding" або "Віртуальні сервери"
3. Додайте правила:
   - **Порт 5000** → `192.168.1.100:5000` (Backend)
   - **Порт 3000** → `192.168.1.100:3000` (Frontend)

### Крок 2: Знайдіть ваш публічний IP

```powershell
# Відкрийте в браузері
https://whatismyipaddress.com/
```

Або:
```powershell
Invoke-RestMethod -Uri "https://api.ipify.org"
```

### Крок 3: Налаштуйте .env файли

**В `backend/.env`:**
```env
HOST=0.0.0.0
PORT=5000
SERVER_URL=http://ВАШ_ПУБЛІЧНИЙ_IP:5000
CORS_ORIGIN=http://ВАШ_ПУБЛІЧНИЙ_IP:3000,http://localhost:3000
```

**В `frontend/.env`:**
```env
VITE_API_URL=http://ВАШ_ПУБЛІЧНИЙ_IP:5000/api
VITE_WS_URL=ws://ВАШ_ПУБЛІЧНИЙ_IP:5000
```

### Крок 4: Використання з інтернету

**З будь-якого комп'ютера:**
- Frontend: `http://ВАШ_ПУБЛІЧНИЙ_IP:3000`
- Backend API: `http://ВАШ_ПУБЛІЧНИЙ_IP:5000/api`

**⚠️ Проблема:** Якщо у вас динамічний IP, адреса буде змінюватися.

---

## 🔒 Варіант 3: Використання DDNS (для динамічного IP)

### Крок 1: Зареєструйтеся на безкоштовному DDNS сервісі

**Варіанти:**
- **No-IP:** https://www.noip.com/ (безкоштовно)
- **DuckDNS:** https://www.duckdns.org/ (безкоштовно)
- **Dynu:** https://www.dynu.com/ (безкоштовно)

### Крок 2: Створіть домен

Наприклад: `vita-edu.ddns.net`

### Крок 3: Налаштуйте клієнт DDNS

Встановіть клієнт на ваш ПК, який автоматично оновлює IP.

### Крок 4: Оновіть .env файли

**В `backend/.env`:**
```env
SERVER_URL=http://vita-edu.ddns.net:5000
CORS_ORIGIN=http://vita-edu.ddns.net:3000
```

**В `frontend/.env`:**
```env
VITE_API_URL=http://vita-edu.ddns.net:5000/api
VITE_WS_URL=ws://vita-edu.ddns.net:5000
```

---

## 🚀 Варіант 4: Використання ngrok (найпростіше для тестування)

### Крок 1: Встановіть ngrok

1. Зареєструйтеся: https://ngrok.com/
2. Завантажте ngrok
3. Отримайте токен авторизації

### Крок 2: Налаштуйте ngrok

```powershell
# Авторизуйте ngrok
ngrok config add-authtoken ВАШ_ТОКЕН

# Запустіть тунель для Backend
ngrok http 5000

# В іншому терміналі запустіть тунель для Frontend
ngrok http 3000
```

### Крок 3: Отримайте URL

ngrok надасть вам публічні URL типу:
- `https://abc123.ngrok-free.app` (Backend)
- `https://xyz789.ngrok-free.app` (Frontend)

### Крок 4: Оновіть .env файли

**В `backend/.env`:**
```env
SERVER_URL=https://abc123.ngrok-free.app
CORS_ORIGIN=https://xyz789.ngrok-free.app
```

**В `frontend/.env`:**
```env
VITE_API_URL=https://abc123.ngrok-free.app/api
VITE_WS_URL=wss://abc123.ngrok-free.app
```

**⚠️ Примітка:** Безкоштовний ngrok має обмеження (URL змінюється при кожному запуску).

---

## 🔐 Налаштування безпеки

### 1. Обмежте доступ до PostgreSQL

**Не відкривайте порт 5432 для інтернету!** PostgreSQL має бути доступний тільки локально.

### 2. Використовуйте сильні паролі

Оновіть паролі в `.env` файлах:
- `JWT_SECRET` - згенеруйте новий
- `JWT_REFRESH_SECRET` - згенеруйте новий
- `DATABASE_URL` - використовуйте сильний пароль для PostgreSQL

### 3. Налаштуйте Firewall

Дозволяйте тільки необхідні порти:
- 3000 (Frontend)
- 5000 (Backend)
- НЕ дозволяйте 5432 (PostgreSQL) для інтернету

### 4. Використовуйте HTTPS (для production)

Для production потрібен SSL сертифікат. Можна використати:
- **Let's Encrypt** (безкоштовно)
- **Cloudflare Tunnel** (безкоштовно, з HTTPS)

---

## 📊 Порівняння варіантів

| Варіант | Складність | Безпека | Стабільність | Вартість |
|---------|-----------|---------|--------------|----------|
| **Локальна мережа** | ⭐ Легко | ⭐⭐⭐ Добре | ⭐⭐⭐ Стабільно | Безкоштовно |
| **Port Forwarding** | ⭐⭐ Середньо | ⭐⭐ Потрібен firewall | ⭐⭐ Залежить від IP | Безкоштовно |
| **DDNS** | ⭐⭐ Середньо | ⭐⭐ Потрібен firewall | ⭐⭐⭐ Стабільно | Безкоштовно |
| **ngrok** | ⭐ Легко | ⭐⭐⭐ Добре | ⭐⭐ Обмежено | Безкоштовно (з обмеженнями) |
| **Railway/Vercel** | ⭐ Легко | ⭐⭐⭐ Відмінно | ⭐⭐⭐ Стабільно | Безкоштовно для demo |

---

## 🎯 Рекомендації

**Для тестування/демонстрації:**
- **Railway + Vercel** — найпростіше і найбезпечніше
- **ngrok** — якщо потрібно швидко показати локальну версію

**Для постійного використання:**
- **Railway + Vercel** — для production
- **DDNS + Port Forwarding** — якщо хочете повний контроль

**Для внутрішньої мережі:**
- **Локальна мережа** — найпростіше, достатньо для офісу/навчального закладу

---

## ⚡ Швидкий старт для локальної мережі

### 1. Знайдіть IP:
```powershell
ipconfig
```

### 2. Оновіть .env:
```env
# backend/.env
HOST=0.0.0.0
SERVER_URL=http://ВАШ_IP:5000
CORS_ORIGIN=http://ВАШ_IP:3000

# frontend/.env
VITE_API_URL=http://ВАШ_IP:5000/api
VITE_WS_URL=ws://ВАШ_IP:5000
```

### 3. Дозвольте порти в Firewall:
```powershell
New-NetFirewallRule -DisplayName "VITA-Edu Backend" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "VITA-Edu Frontend" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

### 4. Запустіть проект:
```powershell
.\start.bat
```

### 5. Доступ з інших ПК:
- `http://ВАШ_IP:3000` (Frontend)
- `http://ВАШ_IP:5000/api-docs` (Swagger)

---

## 📝 Чеклист для налаштування

- [ ] Знайдено IP адресу ПК
- [ ] Оновлено `.env` файли з правильними адресами
- [ ] Налаштовано Windows Firewall (дозволено порти 3000, 5000)
- [ ] Налаштовано Port Forwarding (якщо потрібен доступ з інтернету)
- [ ] Налаштовано DDNS (якщо потрібен стабільний доступ)
- [ ] Перевірено доступ з іншого комп'ютера
- [ ] Налаштовано безпеку (сильні паролі, обмеження доступу)

---

## 🆘 Troubleshooting

### Проблема: Не можу підключитися з іншого ПК
**Рішення:**
1. Перевірте, що обидва ПК в одній мережі
2. Перевірте Firewall (дозволені порти)
3. Перевірте, що сервер запущений (`HOST=0.0.0.0`)
4. Перевірте IP адресу (`ipconfig`)

### Проблема: Порт зайнятий
**Рішення:**
```powershell
# Знайти процес
netstat -ano | findstr :5000
# Зупинити
taskkill /PID <номер> /F
```

### Проблема: CORS помилки
**Рішення:**
- Переконайтеся що `CORS_ORIGIN` містить правильний URL
- Додайте всі можливі URL (localhost, IP, домен)

---

**Готово!** Тепер ваш проект доступний з інших комп'ютерів! 🎉

