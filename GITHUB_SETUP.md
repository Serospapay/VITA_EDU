# GitHub Setup Guide для VITA-Edu

## 🚀 Швидкий старт (автоматично)

**Найпростіший спосіб — використати автоматичний скрипт:**

```powershell
.\setup-github.ps1
```

Скрипт автоматично:
- ✅ Перевірить встановлення Git
- ✅ Налаштує Git конфігурацію (якщо потрібно)
- ✅ Ініціалізує репозиторій
- ✅ Додасть всі файли
- ✅ Створить перший коміт
- ✅ Допоможе налаштувати GitHub remote

---

## 📝 Ручне налаштування

Якщо хочете зробити вручну або скрипт не працює:

### Крок 1: Перезапустити термінал

Після встановлення Git потрібно перезапустити PowerShell або використати Git Bash.

Або додати в PATH:
```powershell
$env:Path += ";C:\Program Files\Git\bin"
```

### Крок 2: Налаштувати Git (перший раз)

```bash
git config --global user.name "Ваше Ім'я"
git config --global user.email "ваш.email@example.com"
```

### Крок 3: Ініціалізувати репозиторій

```bash
# В папці проекту
git init
```

### Крок 4: Додати файли

```bash
git add .
```

### Крок 5: Зробити перший commit

```bash
git commit -m "Initial commit: VITA-Edu project"
```

### Крок 6: Створити репозиторій на GitHub

1. Зайдіть на https://github.com
2. Натисніть "+" → "New repository"
3. Назва: `vita-edu` (або ваша назва)
4. НЕ створюйте README, .gitignore, license (вони вже є)
5. Натисніть "Create repository"

### Крок 7: Підключити до GitHub

```bash
# Замініть USERNAME та REPO_NAME на ваші
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

---

## ✅ Готово! 🎉

Ваш проект на GitHub!

## 📋 Після першого завантаження

Для наступних змін:

```bash
git add .
git commit -m "Опис змін"
git push
```

## 🔐 Автентифікація на GitHub

### Через HTTPS (простий спосіб)
- GitHub автоматично запросить логін/пароль
- Або використайте Personal Access Token замість пароля

### Через SSH (рекомендовано для постійної роботи)
```bash
# Генерація SSH ключа
ssh-keygen -t ed25519 -C "your_email@example.com"

# Додайте ключ до GitHub:
# Settings → SSH and GPG keys → New SSH key
```

---

## 🆘 Проблеми?

**Git не знайдено:**
- Перезапустіть PowerShell після встановлення Git
- Або використайте Git Bash
- Або додайте Git до PATH

**Помилка push:**
- Перевірте правильність URL репозиторію
- Переконайтеся, що репозиторій існує на GitHub
- Перевірте автентифікацію (логін/пароль або SSH ключ)

